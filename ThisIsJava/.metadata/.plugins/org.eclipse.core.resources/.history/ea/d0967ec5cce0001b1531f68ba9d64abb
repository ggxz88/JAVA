package chap09.example;

public class Chatting {
	void startChat(String chatId) {
		String nickName = null;
		nickName = chatId;
		Chat chat = new Chat() {
			@Override
			public void start() {
				while(true) {
					String inpuData = "æ»≥Á«œººø‰";
					String message = "[" + message + "] " + inpuData;
					sendMessage(message);
				}
			}
		};
		chat.start();
	}
}
